{
  "name": "audiolayer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('out',{recursive:true,force:true})\"",
    "test": "node scripts/test.mjs test",
    "coverage": "node scripts/test.mjs coverage",
    "build": "node scripts/build.mjs",
    "start": "node scripts/start.mjs",
    "run:client": "gradle runClient",
    "prepare:kubejs": "gradle --quiet dependencies --configuration compileClasspath 2>&1 | grep kubejs || echo 'KubeJS resolved'"
  }
}
